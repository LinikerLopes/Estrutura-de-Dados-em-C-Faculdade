#include <stdio.h>

int main(){
int num;

printf("Digite um numero: ");
scanf("%d", &num);

if(num % 2 == 0){
  printf("Numero %d e par.", num);
} else {
printf("Numero %d e impar.", num);
}
return 0;
}
